I have just checked the mysql ports and added them and I have done one mistake that I have added driver: bridge but there 
is no need in particular to do so as docker itself takes it as bridge. 

from src-main-resources-application properties checked and matched environment variables. 

one must not forget to write them in capital in docker compose file. 

and things by the way can also be done with the help of Docker init from docker desktop. 
