I have just checked and added ports in mysql container and removed volumes in end from docker compose file. 

and checked environment variable from src->main->resource->application properties and one must write them in capital letter in docker compose file. 

pom.xml -  which are the particular dependencies on can also look there but with the help of docker init once can write Dockerfile and docker compose.yml 
in a single go. 
thank you for your time reading this. 
